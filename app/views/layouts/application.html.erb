<!DOCTYPE html>
<html>
  <head>
    <title>私が宣伝大使</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="d-flex flex-column vh-100">
    <header class="shadow-sm">
      <%= render 'layouts/header' %>
    </header>
    <main>
      <% if flash[:notice] %>
        <div class="alert alert-info text-center" role="alert"><strong><%= notice %></strong></div>
      <% end %>
      <% if flash[:alert] %>
        <div class="alert alert-danger text-center" role="alert"><strong><%= alert %></strong></div>
      <% end %>
      <%= yield %>
    </main>
    <footer>
      <p class="text-right">Copyright(C) 私が宣伝大使. All Rights Researved.</p>
    </footer>
  </body>
</html>